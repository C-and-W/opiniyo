<%= form_for(@survey) do |f| %>
  <% if @survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% @survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :write_up, :placeholder=>'Type here...', class: 'form-panel', :style=>"resize:none; height: 150px; padding-top: 30px; padding-right: 30px; margin-top: 8vh;" %>
  </div>

  <div class="field">
    <div style="margin-top: 5vh; font-family: 'Roboto Slab', serif; font-weight:bold; font-size: 35px; line-height: 30px; color:black;">Link</div>
    <%= f.text_field :link, :placeholder=>'Type here...', class: 'form-panel', :style=>"margin-top: 2vh;" %>
  </div>

  <div class="actions">
    <%= f.submit 'Post', class: 'form-panel form-submit' %>
  </div>

<% end %>
