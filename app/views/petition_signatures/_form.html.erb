<%= form_for(@petition_signature) do |f| %>
  <% if @petition_signature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@petition_signature.errors.count, "error") %> prohibited this petition_signature from being saved:</h2>

      <ul>
      <% @petition_signature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :petition_id %><br>
    <%= f.number_field :petition_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
